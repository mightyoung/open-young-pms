## Design Context

### Users
工程项目管理人员：现场人员（问题上报、任务执行）、项目负责人（项目全局管理）、部门/公司领导（监控决策）

### Brand Personality
Industrial Executive — 专业、高效、清晰、数字化的管理平台。冷静克制的深色界面，状态色彩驱动信息传达，不做任何装饰性用色。

### Aesthetic Direction
- 深色专业界面，信息密度高但有清晰层次
- 状态驱动色彩（绿/黄/红），无纯装饰色
- 左侧色条传达状态（不是卡片边框，不是渐变）
- 渐进式交互，短路径完成任务

### Design Principles
1. **状态优先** — 色彩传达状态，不是装饰。状态色出现在左侧色条，不做顶部渐变。
2. **效率至上** — 最短路径完成任务。无多余动画（<300ms），动效服务于反馈而非美观。
3. **信息分层** — 重要信息突出（大数字、粗体），次要信息降低对比度折叠。
4. **响应灵敏** — framer-motion 处理交互反馈，CSS 仅用于静态样式。

---

## Design Tokens

### Color (oklch)
```
--color-bg:            oklch(13% 0.01 250)
--color-surface:       oklch(18% 0.01 250)
--color-surface-2:     oklch(22% 0.01 250)
--color-border:        oklch(28% 0.01 250 / 0.2)
--color-text-primary:  oklch(92% 0.01 250)
--color-text-secondary:oklch(65% 0.01 250)
--color-text-muted:    oklch(42% 0.01 250)

--color-primary:       oklch(60% 0.15 250)
--color-safe:          oklch(70% 0.18 145)
--color-warning:       oklch(80% 0.16 85)
--color-danger:       oklch(65% 0.2 25)
--color-info:          oklch(65% 0.13 250)
```

### Typography
- **Headings/UI labels**: "Plus Jakarta Sans", system-ui — 现代、几何感、不俗气
- **Big numbers/data**: "Outfit", system-ui — 大数字显示，tabular-nums
- **NOT Inter, Roboto, Open Sans, DM Sans, Nunito** — 这些太常见

### Spatial
- Base unit: 4px
- Component gap: 16px
- Inner padding: 16-20px
- Border radius: 10-12px max（不用大圆角卡片）

### Motion
- Entrance: ease-out-quart `[0.25, 1, 0.5, 1]`, 0.3s
- Hover feedback: ease-out, 0.15s
- Properties: opacity + transform only（不用 scaleX 做数字动画）

### Anti-Patterns (AI Slop)
- ❌ 渐变作为装饰（gradient top bar）
- ❌ glassmorphism / backdrop-blur
- ❌ 居中对称布局
- ❌ 圆角过大的卡片（>16px）
- ❌ bounce/elastic 动效
- ❌ Inter 系统默认字体
- ❌ 卡片包裹一切（border-radius 12px 背景卡片）

---

## Implementation Rules

### React/Vercel Best Practices Applied
1. **rerender-no-inline-components** — StepDot, StepConnector, ProgressRow, StatusStepper 全部提取到模块层
2. **rerender-functional-setstate** — TrendIcon 计算提取到 `getTrendMeta()` 模块函数
3. **bundle-localstorage-schema** — DRAFT_KEY 带版本前缀 (`pms_capture_draft:v1`)
4. **bundle-barrel-imports** — 只导入用到的 lucide-react 图标
5. **rerender-memo** — 所有组件用 `memo()` 包装

### Layout Strategy
- 不再用 `border: 1px solid` 的卡片包裹，改用 `border-left: 3px solid {color}` 色条
- 列表项之间通过间距（gap）自然分隔，不强制每个元素卡片化
