// Backend API integration — wire up once reports router is in place (Layer 3).
// import { api } from '../../api'

export const reportsFeatureApi = {
  // list: (params) => api.get('/reports', params),
  // submit: (data) => api.post('/reports', data),
  // For now, use mock data via the hook.
}

export default reportsFeatureApi
